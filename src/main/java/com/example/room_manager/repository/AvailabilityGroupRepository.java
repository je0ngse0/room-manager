package com.example.room_manager.repository;

import com.example.room_manager.domain.AvailabilityGroup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AvailabilityGroupRepository extends JpaRepository<AvailabilityGroup, Long> {

}
